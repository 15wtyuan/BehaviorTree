diff a/Assets/BehaviorTree/Samples/TreeTestSample.cs b/Assets/BehaviorTree/Samples/TreeTestSample.cs	(rejected hunks)
@@ -55,13 +55,14 @@
             _treeC = new BehaviorTreeBuilder(sharedBlackboard)
                 .Name("Basic")
                 .Sequence()
-                    .Condition("Custom Condition", () => _condition)
+                    //.Attack(sharedBlackboard.Get<SharedInt>("attackId"))
                     .Do("Continue", () => _condition ? TaskStatus.Continue : TaskStatus.Success)
                 .End()
                 .Build();
         }
 
-        private void Update () {
+        private void Update()
+        {
             // Update our tree every frame
             _treeA.Tick();
             _treeB.Tick();
